(ns ember-grep.core
  (:gen-class)
  (:require [clj-http.client :as client]))

(defn authenticate
  "Takes ")

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
